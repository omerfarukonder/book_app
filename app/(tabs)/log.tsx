import { View, Text } from 'react-native';

// This is a placeholder screen that exists to register the tab route.
// The actual log action is handled by the tab press listener in _layout.tsx
// which opens the log-modal instead.
export default function LogPlaceholder() {
  return <View />;
}
